"""Reference simulations demonstrating evosim (per SPEC).

- :mod:`evosim.examples.conway` — Conway's Game of Life (minimal field-only sim, no genome).
- :mod:`evosim.examples.foragers` — evolving foragers (full agent-based ALife slice).
- :mod:`evosim.examples.ga_benchmark` — classic genetic algorithm (explicit-fitness path).

Each module exposes a ``build``/``run`` API and a ``main()`` for ``python -m`` execution.
"""
